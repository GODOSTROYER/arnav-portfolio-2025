/* /dev — kept as a noindex preview URL now that the mobile experience ships
   on the real home page. Same component, so the two can never drift; use it
   to trial changes before they reach `/`. */

export { default } from "../page"
